name: Deploy Spring Boot to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Clone source code từ GitHub
      - name: 1️⃣ Checkout repository
        uses: actions/checkout@v3

      # 1️⃣.5 Thiết lập JDK 21
      - name: 1️⃣.5 Set up JDK 21
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin' # Hoặc 'zulu', 'adopt'
          java-version: '21'

      # 2️⃣ Build Spring Boot Application
      - name: 2️⃣ Build Spring Boot Application
        run: |
          chmod +x mvnw  # Nếu dùng Maven Wrapper
          ./mvnw clean package -DskipTests  # Build ứng dụng

      # 3️⃣ Verify JAR file exists
      - name: 3️⃣ Verify JAR file exists
        run: |
          if [ ! -f target/myapp.jar ]; then
            echo "❌ Lỗi: Không tìm thấy target/myapp.jar"
            exit 1
          fi

      # 4️⃣ Build Docker Image
      - name: 4️⃣ Build Docker Image
        run: |
          docker build -t myapp:latest .
          docker save myapp:latest -o myapp.tar

      # 5️⃣ Copy Docker Image lên VPS
      - name: 5️⃣ Copy image to VPS
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          source: "myapp.tar"
          target: "/home/user/app/"

      # 6️⃣ Deploy trên VPS mà không bị trùng port
      - name: 6️⃣ Load Docker Image & Restart Container
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            sudo systemctl start docker || sudo service docker start
            cd /home/user/app
            docker load -i myapp.tar
            docker stop spring_boot || true
            docker rm spring_boot || true
            docker run -d --name spring_boot -p 8080:8080 --restart always myapp:latest

      # 7️⃣ Gửi thông báo lên Discord khi deploy thành công
      - name: 7️⃣ Notify Discord for success
        env:
          WEBHOOK: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          if [ -n "$WEBHOOK" ]; then
            curl -X POST -H "Content-Type: application/json" \
            -d '{"content": "🚀 Deploy Spring Boot thành công lên VPS!"}' \
            "$WEBHOOK"
          fi

#  failure:
#    runs-on: ubuntu-latest
#    steps:
#      - name: 8️⃣ Notify Discord for failure
#        env:
#          WEBHOOK: ${{ secrets.DISCORD_WEBHOOK_URL }}
#        run: |
#          if [ -n "$WEBHOOK" ]; then
#            curl -X POST -H "Content-Type: application/json" \
#            -d '{"content": "⚠️ Deploy Spring Boot thất bại!"}' \
#            "$WEBHOOK"
#          fi
